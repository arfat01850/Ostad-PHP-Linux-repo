<?php
include_once "functions.php";

$n= 1;
echo "The factorial of {$n} is $result".factorical($n);

// function factorial($number) {
//     if ($number == 0) {
//         return 1;
//     }
//     return $number * factorial($number - 1);
// }

// $num = 5;
// $result = factorial($num);

// echo "The factorial of $num is $result";




/*write a function that takes an array of integer and a target sum as input, 
and returns a boolean indicating whether exist a pair of integers in the array 
that add up to the target sum. you can assume the array has at last two elements 
and all elements are unique. 
use a loop in your solution  input [5,7,1,2]
*/